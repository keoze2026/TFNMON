{
  "name": "tfn-checker",
  "version": "1.0.0",
  "description": "Repeatedly loads TFN/scam landing pages (simulating auto-refreshes that open in new tabs), extracts the toll-free/support number rendered on the page, and prints only numbers not previously seen.",
  "main": "index.js",
  "type": "commonjs",
  "bin": {
    "tfn-checker": "index.js"
  },
  "scripts": {
    "start": "node index.js",
    "postinstall": "playwright install chromium"
  },
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.47.0"
  }
}
